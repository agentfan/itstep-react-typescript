import React from 'react';

interface ItemProps {
    item: ListItem
}

const Item: React.FC<ItemProps> = (props) => {
    const {item} = props;
    
    return (
        <div>
            45454545
        </div>
    );
}

export default Item;